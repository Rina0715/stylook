<!--<div class="rating-box mt-3">-->
<!--  <%#= form_with model: post_comment, url: article_post_comments_path(article), local: true do |f| %>-->
<!--    <div class="main-raiting">-->
<!--      <div class="raitings mb-3"></div>-->
<!--    </div>-->
<!--    <%#= f.text_area :comment, class:"form-control book_body" %>-->
<!--    <%#= f.submit "送信", class:"btn btn-outline-dark comment-submit float-right" %>-->
<!--  <%# end %>-->
<!--</div>-->
<h6 class="more" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">もっと見る....</h6>
<% post_comments.first(2).each do |post_comment| %>
  <div class="media border mb-3">
    <%= attachment_image_tag post_comment.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class:"mt-3 rounded-circle"; %>
    <div class="media-body ml-4 p-3">
      <div class="text-area">
        <p><%= post_comment.comment %></p>
      </div>
      <div class="text-right">
        <% if post_comment.user == current_user %>
          <%= link_to "削除", article_post_comment_path(post_comment.article, post_comment), method: :delete, class: "btn btn-secondary" %>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
<div class="collapse" id="collapseExample">
  <% post_comments.offset(2).each do |post_comment| %>
    <div class="media border mb-3">
      <%= attachment_image_tag post_comment.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class:"mt-3 rounded-circle"; %>
      <div class="media-body ml-4 p-3">
        <div class="text-area">
          <p><%= post_comment.comment %></p>
        </div>
        <div class="text-right">
          <% if post_comment.user == current_user %>
            <%= link_to "削除", article_post_comment_path(post_comment.article, post_comment), method: :delete, class: "btn btn-secondary" %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>