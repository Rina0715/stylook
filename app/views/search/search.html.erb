<div class="container">
  <div class="col-xs-12">
    <% if @model == "user" %>
    <h2>Users search for '<%= @value %>'</h2>
    <% @datas.each do |user| %>
      <tbody>
        <tr>
          <th>
            <%= attachment_image_tag @article.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", class:"mt-3 rounded-circle"; %>
          </th>
          <th>
            <%= user.name %>
          </th>
        </tr>
      </tbody>
    <% end %>
    <% elsif @model == "genre" %>
    <h2>Genres search for '<%= @value %>'</h2>
    <% @datas.each do |genre| %>
      <tbody>
        <tr>
          <th>
            <%= link_to article_path(article.id) do %>
              <%= attachment_image_tag article, :image, size:'300x300', class:"card-img-top"; %>
            <% end %>
          </th>
        </tr>
      </tbody>
    <% end %>
    <% end %>
  </div>
</div>